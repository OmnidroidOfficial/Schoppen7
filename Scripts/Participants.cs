> Participants.cs 0.0 

using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class Participants : MonoBehaviour
{
    static public int ParticipantsCount;

    // Start is called before the first frame update

    static public void OnlineParticipants() 
    { 
        // if HasSpectators == true => if Aye = false => spectator may overtake player number
    }

    static public void OfflineParticipants()
    {

    }

    // Update is called once per frame
    
}
